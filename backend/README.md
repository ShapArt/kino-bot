# Backend (placeholder)

Planned: FastAPI service for TMDB proxy/search, user/watchlist storage (Postgres), rate limits, miniapp auth via initData, reminder jobs. Add pyproject/requirements after scaffold.
